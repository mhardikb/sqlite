public class database extends SQLiteOpenHelper {

    public database(@Nullable Context context) {
        super(context, "database", null, 1);
    }

    @Override
    public void onCreate(SQLiteDatabase db) {
        String table = "create table db (id integer primary key,name text,email text,password text,city text)";
        db.execSQL(table);
    }

    @Override
    public void onUpgrade(SQLiteDatabase db, int oldVersion, int newVersion) {

    }

    public void insertdata(String name, String email, String password, String city) {


        String qry = "insert into db values (null,'"+name+"','"+email+"','"+password+"','"+city+"')";
        SQLiteDatabase dbb = this.getWritableDatabase();
        dbb.execSQL(qry);

    }

public boolean select(String email, String password) {

        SQLiteDatabase database = this.getWritableDatabase();
        Cursor cursor = database.rawQuery("select * from db where email = '"+email+"' and password = '"+password+"'",null);
      if (cursor.getCount()>0){

              return true;
      }
      else {
             return  false; }
    }

    public Cursor getdata() {

        SQLiteDatabase database = this.getWritableDatabase();
        Cursor cursor = database.rawQuery("select * from db",null);
        return cursor;

    }

    public void delete(String s) {

        SQLiteDatabase database =this.getWritableDatabase();
        String s1 = "delete from db where id = '"+s+"'";
        database.execSQL(s1);
    }

    public void updatedata(String ss, String ss1, String ss2, String ss3, String ss4) {
        SQLiteDatabase database = this.getWritableDatabase();
        String update = "update db set name='"+ss1+"',email='"+ss2+"',password='"+ss3+"',city='"+ss4+"' where id='"+ss+"'";
        database.execSQL(update);



    }
    
    
    
delete and updta in database class

  public void delete(String s) {

        SQLiteDatabase database =this.getWritableDatabase();
        String s1 = "delete from db where id = '"+s+"'";
        database.execSQL(s1);
    }

    public void updatedata(String ss, String ss1, String ss2, String ss3, String ss4) {
        SQLiteDatabase database = this.getWritableDatabase();
        String update = "update db set name='"+ss1+"',email='"+ss2+"',password='"+ss3+"',city='"+ss4+"' where id='"+ss+"'";
        database.execSQL(update);



    }
}

}




main activity

  database database = new database(this);
      b1.setOnClickListener(new View.OnClickListener() {
          @Override
          public void onClick(View v) {
              database.insertdata(t1.getText().toString(),t2.getText().toString(),t3.getText().toString(),t4.getText().toString());

                t1.setText("");
                t2.setText("");
                t3.setText("");
                t4.setText("");



          }
      });
      
      
      
      
      
      view data
      
      
 EditText e1,e2;
Integer id;
ArrayList<Integer> d = new ArrayList<>();
ArrayList<String> d1 = new ArrayList<>();
ArrayList<String> d2 = new ArrayList<>();
ArrayList<String> d3 = new ArrayList<>();
ArrayList<String> d4 = new ArrayList<>();
Button b1;
dbhelper dbhelper = new dbhelper(this);
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main2);
        e1 = findViewById(R.id.t1);
        e2 = findViewById(R.id.t2);
        b1 = findViewById(R.id.button);
        b1.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                String s1 = e1.getText().toString();
                String ss1 = e2.getText().toString();
                boolean cursor = dbhelper.select(s1,ss1);

                if (cursor==true){
                    Cursor cursor1 = dbhelper.getdata(s1);
                    while (cursor1.moveToNext()){
                        d.add(cursor1.getInt(0));
                        d1.add(cursor1.getString(1));
                        d2.add(cursor1.getString(2));
                        d3.add(cursor1.getString(3));
                        d4.add(cursor1.getString(4));
                    }
                    id = d.get(0);
                    String fn = d1.get(0);
                    String email = d2.get(0);
                    String pw = d3.get(0);
                    String city = d4.get(0);

                    Intent intent = new Intent(MainActivity2.this,MainActivity3.class);
                    intent.putExtra("id",id.toString());
                    intent.putExtra("name",fn);
                    intent.putExtra("email",email);
                    intent.putExtra("pw",pw);
                    intent.putExtra("city",city);
                    startActivity(intent);
                }
                else
                {
                    Toast.makeText(MainActivity2.this, "failed", Toast.LENGTH_SHORT).show();
                }
            }
        });

    }


}


         view data
      EditText e1,e2,e3,e4;
TextView t;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main3);
        e1 = findViewById(R.id.getname);
        e2 = findViewById(R.id.getemail);
        e3 = findViewById(R.id.getpw);
        e4 = findViewById(R.id.getcity);
        t = findViewById(R.id.t);



        String s1 = getIntent().getStringExtra("id");
        String s2 = getIntent().getStringExtra("name");
        String s3 = getIntent().getStringExtra("email");
        String s4 = getIntent().getStringExtra("pw");
        String s5 = getIntent().getStringExtra("city");

        t.setText(s1);
        e1.setText(s2);
        e2.setText(s3);
        e3.setText(s4);
        e4.setText(s5);


now for update

        database database = new database(this);

        b.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                String ss = e1.getText().toString();
                String ss1 = e2.getText().toString();
                String ss2 = e3.getText().toString();
                String ss3 = e4.getText().toString();
                String ss4 = e5.getText().toString();

                database.updatedata(ss,ss1,ss2,ss3,ss4);

                Intent intent=new Intent(update.this,viewdata.class);
                startActivity(intent);
                finish();

            }
        });



